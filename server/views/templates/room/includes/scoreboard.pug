.table-responsive
  table.table.table-condensed
    thead
      tr
        th Player
        th Score
    tbody
      tr(ng-repeat="player in vm.room.players | orderBy:player.score")
        th(scope="row" ng-bind="::player.username")
        td(ng-bind="player.score")